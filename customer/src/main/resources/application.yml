server:
  port: 8080
spring:
  application:
    name: customer
  datasource:
    password: 'kaguayr'
    url: jdbc:postgresql://localhost:5432/customer
    username: 'okaru'
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true